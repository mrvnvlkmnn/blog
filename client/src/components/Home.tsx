import React from 'react';
import { NavLink } from 'react-router-dom';
import { Button } from 'primereact/button';

export const Home = () => {
    return(
        <div className="home">
            home
        </div>
    )
}